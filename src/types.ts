// Barrel export — re-exports all types from the modular structure.
// Import from '@/types' as before; the implementation is now split across
// domain-specific modules in src/types/*.ts for better maintainability.

export * from './types/index';
